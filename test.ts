import emailjs from '@emailjs/nodejs';
// Wait, is @emailjs/nodejs installed? I'll check it, but let's just use @emailjs/browser first if it works, or I'll just change the error logging in footer.tsx which is easier.
